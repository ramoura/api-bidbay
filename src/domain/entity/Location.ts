export default class Location {
    constructor(readonly lat: number, readonly lng: number, readonly address: string, readonly city: string, readonly state: string, readonly zip_code: number) {
    }
}